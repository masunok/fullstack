<?xml version="1.0" encoding="UTF-8"?>
<main class="container mx-auto px-4 py-8">
  <div class="max-w-md mx-auto">
    <div class="bg-white p-8 rounded-lg shadow-lg border">
      <!-- Header -->
      <div class="text-center mb-8">
        <h1 class="text-2xl font-bold text-gray-800 mb-2">회원가입</h1>
        <p class="text-gray-600">새로운 계정을 만드세요</p>
      </div>
      
      <!-- Signup Form -->
      <form class="signup-form" action="/auth/signup" method="POST">
        <!-- CSRF Token -->
        <input type="hidden" name="csrf_token" value="{csrf_token}"/>
        
        <!-- Email -->
        <div class="form-group mb-4">
          <label for="email" class="block text-sm font-medium text-gray-700 mb-2">이메일</label>
          <input 
            type="email" 
            id="email" 
            name="email" 
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            placeholder="이메일을 입력하세요"
            required
          />
          <div class="error-message text-red-500 text-xs mt-1 hidden" id="email-error"></div>
        </div>
        
        <!-- Username -->
        <div class="form-group mb-4">
          <label for="username" class="block text-sm font-medium text-gray-700 mb-2">사용자명</label>
          <input 
            type="text" 
            id="username" 
            name="username" 
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            placeholder="사용자명을 입력하세요"
            required
          />
          <div class="help-text text-gray-500 text-xs mt-1">영문, 숫자, 언더스코어 사용 가능</div>
          <div class="error-message text-red-500 text-xs mt-1 hidden" id="username-error"></div>
        </div>
        
        <!-- Display Name -->
        <div class="form-group mb-4">
          <label for="display_name" class="block text-sm font-medium text-gray-700 mb-2">표시 이름 (선택사항)</label>
          <input 
            type="text" 
            id="display_name" 
            name="display_name" 
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            placeholder="표시할 이름을 입력하세요"
          />
          <div class="error-message text-red-500 text-xs mt-1 hidden" id="display_name-error"></div>
        </div>
        
        <!-- Password -->
        <div class="form-group mb-4">
          <label for="password" class="block text-sm font-medium text-gray-700 mb-2">비밀번호</label>
          <input 
            type="password" 
            id="password" 
            name="password" 
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            placeholder="비밀번호를 입력하세요"
            required
          />
          <div class="help-text text-gray-500 text-xs mt-1">영문자, 숫자, 특수문자 포함 10자리 이상</div>
          <div class="error-message text-red-500 text-xs mt-1 hidden" id="password-error"></div>
        </div>
        
        <!-- Password Confirm -->
        <div class="form-group mb-6">
          <label for="password_confirm" class="block text-sm font-medium text-gray-700 mb-2">비밀번호 확인</label>
          <input 
            type="password" 
            id="password_confirm" 
            name="password_confirm" 
            class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
            placeholder="비밀번호를 다시 입력하세요"
            required
          />
          <div class="error-message text-red-500 text-xs mt-1 hidden" id="password_confirm-error"></div>
        </div>
        
        <!-- Terms Agreement -->
        <div class="form-group mb-6">
          <label class="flex items-center">
            <input type="checkbox" name="agree_terms" required class="mr-2"/>
            <span class="text-sm text-gray-700">이용약관 및 개인정보처리방침에 동의합니다</span>
          </label>
        </div>
        
        <!-- Submit Button -->
        <button 
          type="submit" 
          class="w-full bg-blue-600 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
        >
          회원가입
        </button>
        
        <!-- General Error Message -->
        <div class="error-message text-red-500 text-sm text-center mt-4 hidden" id="general-error"></div>
      </form>
      
      <!-- Footer Links -->
      <div class="text-center mt-6">
        <p class="text-sm text-gray-600">
          이미 계정이 있으신가요? 
          <a href="/login" class="text-blue-600 hover:text-blue-800">로그인</a>
        </p>
      </div>
    </div>
  </div>
</main>